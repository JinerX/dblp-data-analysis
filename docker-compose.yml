version: '3.8'

services:
  notebook:
    build: .
    volumes:
      - ./src:/app/src
      - ./notebooks:/app/notebooks
      - ./data:/app/data
      - ./pyproject.toml:/app/pyproject.toml
    ports:
      - "8888:8888"
    environment:
      - PYTHONPATH=/app/src
